= render 'nav_bar'
.container
	.row
		.col-md-5
			%h1 New Sitter

			= form_for @new_sitter, url: "/sitters" do |f|
				= render 'form', f: f
			%br
			= button_to 'Back', sitters_path, method: "get"