= render 'nav_bar'
.container
	.row
		.col-md-5
			%h1 New Family

			= form_for @new_family, url: "/families" do |f|
				= render 'form', f: f
			%br
			= button_to 'Back', families_path, method: "get"