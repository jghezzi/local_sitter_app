= form_for @sit, url: "/sits/#{@sit.id}", method: :put do |f|
	= f.label "Date of sit"
	= f.date_field :date
	= f.label "Start Time"
	= f.time_select :start_time
	= f.label "End Time"
	= f.time_select :end_time
	= f.label "Status"
	= f.text_field :status
	= f.submit "EDIT"
